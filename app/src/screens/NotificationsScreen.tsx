import React,{useCallback,useState}from'react';import{FlatList,Text}from'react-native';import{useFocusEffect}from'@react-navigation/native';import{api}from'../api/client';import{Button,Card,Screen,Title}from'../components/UI';import{colors}from'../theme';export function NotificationsScreen(){const[data,setData]=useState<any[]>([]);const load=useCallback(()=>api.get('/notifications').then(r=>setData(r.data)),[]);useFocusEffect(useCallback(()=>{load()},[load]));return <Screen><Title>Notifications</Title><Button title="Mark all read" onPress={()=>api.patch('/notifications/read-all').then(load)}/><FlatList data={data} keyExtractor={x=>x.id} renderItem={({item})=><Card><Text style={{fontWeight:item.readAt?'500':'900',color:colors.text}}>{item.title}</Text><Text style={{color:colors.muted,marginTop:5}}>{item.body}</Text></Card>}/></Screen>}
