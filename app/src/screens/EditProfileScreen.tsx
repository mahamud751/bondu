import React,{useEffect,useState}from'react';import{Alert,ScrollView}from'react-native';import{api}from'../api/client';import{Button,Field,Screen,Title}from'../components/UI';export function EditProfileScreen({navigation}:{navigation:any}){const[displayName,setName]=useState(''),[bio,setBio]=useState(''),[city,setCity]=useState('');useEffect(()=>{api.get('/users/me').then(({data})=>{setName(data.profile?.displayName??'');setBio(data.profile?.bio??'');setCity(data.profile?.city??'')})},[]);const save=async()=>{try{await api.patch('/users/me/profile',{displayName,bio,city});Alert.alert('Saved','Profile updated.');navigation.goBack()}catch(e:any){Alert.alert('Could not save',e.response?.data?.message??'Try again')}};return <Screen><ScrollView><Title>Edit profile</Title><Field placeholder="Display name" value={displayName} onChangeText={setName}/><Field placeholder="Bio" value={bio} onChangeText={setBio} multiline/><Field placeholder="City" value={city} onChangeText={setCity}/><Button title="Save profile" disabled={!displayName.trim()} onPress={save}/></ScrollView></Screen>}
